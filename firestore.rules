rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Top level rule: allow read/write to all collections if authenticated.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
